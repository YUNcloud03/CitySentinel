# 環境變數範本
# ---------------------------------------------------------------------------
# 使用方式：複製本檔為 .env 並填入自己的金鑰。
#   cp .env.example .env
#
# ⚠️ .env 已列入 .gitignore，請勿將含有真實金鑰的檔案提交至版控。
# ⚠️ 本檔僅為範本，不得填入真實金鑰。
# ---------------------------------------------------------------------------

# --- LLM Provider（兩者皆為選用）---
# 優先序：ANTHROPIC_API_KEY > OPENAI_API_KEY > 確定性模板
# 兩者皆未設定時，系統自動降級為確定性模板，所有功能仍可完整展示，
# 僅「LLM 生成」的文字改由模板產出（UI 會標示生成來源）。

# ANTHROPIC_API_KEY=your-anthropic-key-here
# OPENAI_API_KEY=your-openai-key-here

# --- 測試用 ---
# 設為 1 可強制停用 LLM，確保測試結果具確定性（conftest.py 已自動設定）
# CITY_LLM_DISABLED=1
