# 環境變數範本
# ---------------------------------------------------------------------------
# 使用方式：複製本檔為 .env 並填入自己的金鑰。
#   cp .env.example .env
#
# ⚠️ .env 已列入 .gitignore，請勿將含有真實金鑰的檔案提交至版控。
# ⚠️ 本檔僅為範本，不得填入真實金鑰。
# ---------------------------------------------------------------------------

# --- LLM Provider（全部選用）---
# 優先序＝backend/app/llm/client.py 的 PROVIDERS 宣告順序，取第一個有金鑰者：
#   bedrock > anthropic > openai > google > deepseek > groq > xai > openrouter
# 全部未設定時系統自動降級為確定性模板，所有功能仍可完整展示，
# 僅「LLM 生成」的文字改由模板產出（UI 會標示生成來源）。

# AWS Bedrock（黑客松環境，預設模型 Kimi K2.5）
# 金鑰具區域性：端點區域必須與金鑰簽發區域一致，否則會 AccessDenied。
# 短期金鑰最長 12 小時，過期需重新產生。
# AWS_BEARER_TOKEN_BEDROCK=bedrock-api-key-xxxxx

# ANTHROPIC_API_KEY=your-anthropic-key-here
# OPENAI_API_KEY=your-openai-key-here
# GOOGLE_API_KEY=your-google-ai-studio-key-here     # 或 GEMINI_API_KEY
# DEEPSEEK_API_KEY=your-deepseek-key-here
# GROQ_API_KEY=your-groq-key-here
# XAI_API_KEY=your-xai-key-here
# OPENROUTER_API_KEY=your-openrouter-key-here

# --- 覆寫（選用，不必改程式碼）---
# CITY_LLM_PROVIDER=bedrock                 # 指定廠牌，跳過自動偵測
# CITY_LLM_MODEL=moonshotai.kimi-k2.5       # 覆寫模型（Kimi 不可加 us. 前綴）
# CITY_LLM_BASE_URL=https://bedrock-mantle.us-west-2.api.aws/v1
# CITY_LLM_API_KEY=...                      # 搭配 BASE_URL 可接任何 OpenAI 相容服務

# --- 限流（選用）---
# Bedrock mantle 端點不設 RPM 上限（只管 TPM），故預設 0＝不做固定間隔限流；
# 429／逾時一律自動指數退避重試。若實測遭限流，可設為 0.9 啟用每秒一次的節流。
# CITY_LLM_MAX_RPS=0

# --- 測試用 ---
# 設為 1 可強制停用 LLM，確保測試結果具確定性（conftest.py 已自動設定）
# CITY_LLM_DISABLED=1
